# Leaderboard — DB2Model

Единая сравнительная таблица (DoD недель 5–6). Все числа — на одном split:
**91 вопрос `bird_large`** по трём базам `financial / toxicology / codebase_community`,
target — **Qwen2.5-Coder-3B-Instruct** (4-bit), один исполнитель SQL, один парсер.

Оси сравнения из плана: **EX** (точность), **VES** (эффективность корректных запросов),
**prompt-токены/запрос** (бюджет контекста — ключевая метрика задачи).

## Главный итог (числа — из «Канонического замера» ниже)

| Арм | Ветка | Схема в промпте | EX | prompt-ток. |
|---|---|---|---|---|
| zeroshot (контроль) | — | нет | 5.49% | 90 |
| baseline **3B** | — | да | 24.18% | 288 |
| **lora 3B** (qwen27b_sql_only, 3 сида) | SFT/LoRA | нет | **30.77% ± 1.55** | **90** |
| baseline **7B** | — | да | 30.77% | 288 |
| **lora 7B** (qwen27b_sql_only, 3 сида) | SFT/LoRA | нет | **41.03% ± 1.37** | **90** |
| light-RAG | RAG (напарница) | подграф | _TBD_ | _TBD_ |

**Знание в весах обгоняет знание в контексте на обоих размерах: +10.3 п.п. на 7B
(41.03 против 30.77) и +6.6 п.п. на 3B (30.77 против 24.18)** — при контексте в 3.2 раза
меньше и генерации в 1.44 раза быстрее. Контроль `zeroshot` (5.49%) показывает, что это
заслуга адаптера, а не базовой модели.

## Ранний замер на train 1171 (учитель Qwen-7B) — история кривой объёма

Исторический блок: на нём отрабатывались сиды, утечка и ablation по объёму данных.
Числа **воспроизведены 11.08 в точности** (см. кривую объёма ниже) — стенд стабилен;
отличаются они от канона не замером, а учителем (Qwen-7B) и набором данных (1171).

| Арм | Ветка | Схема в промпте | EX | VES | prompt-ток. |
|---|---|---|---|---|---|
| zeroshot (контроль) | — | нет | 5.49% | 5.35% | 90 |
| baseline | — | да | 23.08% | 23.51% | 288 |
| **lora 3B (synth_1171)** | SFT/LoRA | нет | **27.84% ± 0.52** | ≈ EX | **90** |
| lora (synth_347) | SFT/LoRA | нет | 16.85% ± 1.37 | 16.91% ± 1.35 | 91 |

lora (1171) 27.84% ± 0.52 vs
baseline 23.08% = **+4.8 п.п.** при контексте в 3.2 раза меньше (90 vs 288 токенов).
Контроль `zeroshot` 5.49% доказывает, что это заслуга адаптера: `lora − zeroshot` =
**+22 п.п.** чистого вклада обучения. Прирост от объёма данных: lora 16.85 (347) →
27.84 (1171). baseline детерминирован (greedy); lora усреднена по 3 сидам.

- **3 сида на 1171** (0/1/2, `kaggle_seeds.ipynb`): EX 27.47 / 27.47 / 28.57 → 27.84% ± 0.52.
  Ранее публиковавшийся одиночный seed 42 давал 29.67% — выше среднего (наглядно, зачем повторы).
  Разрыв с baseline (+4.8 п.п.) кратно больше σ.
- **VES ≈ EX во всех армах** — корректные предсказания исполняются примерно с той же скоростью,
  что gold, так что эффективность не различает армы, ранжирование по VES то же, что по EX.
  Токены — единственная ось, где lora выигрывает (90 vs 288), но у локальной 3B это почти бесплатно.
- **Утечка train→eval** проверена `check_leakage.py`: 2 вопроса (q320, q323) с точным gold-SQL-твином
  в обучении. Без них (3 сида) lora 28.09 ± 1.12 vs baseline 22.47 (89 вопросов) = +5.6 п.п. — знак
  сохраняется и даже растёт (заученный ответ сработал лишь на одном сиде).
- **light-RAG** — строка ветки напарницы, сводится в конце (общий split уже согласован).

## Канонический замер (пересчёт 10.08.2026)

Все армы прогнаны через `bird_large_filtered.json` — это те же 91 вопрос, что и в
`data/bird_large.json` (подмножество, gold-SQL совпадает буква в букву), поэтому оба
файла дают **идентичный EX**. Файл теперь лежит в репозитории (`data/bird_large_filtered.json`),
и вся таблица ниже воспроизводится локально одной командой на арм.

| Арм (lora = знание в весах) | Учитель / данные | EX | VES | simple / moder. / chall. |
|---|---|---|---|---|
| zeroshot (контроль) | — | 5.49% | 6.76 | 11.4 / 3.0 / 0 |
| baseline **3B** | схема в контексте | 24.18% | 24.51 | 28.6 / 24.2 / 17.4 |
| new_lora **3B** | Qwen-27B, 2699, +мультизадачи (1 сид) | 28.57% | 29.24 | 40.0 / 18.2 / 26.1 |
| qwen27b_sql_only **3B** | Qwen-27B, 1096 (3 сида) | **30.77% ± 1.55** | **31.10 ± 1.10** | — |
| baseline **7B** | схема в контексте | 30.77% | 30.55 | 45.7 / 21.2 / 21.7 |
| reasoning **7B** | Qwen-27B, 1096 CoT (1 сид) | 30.77% | 29.51 | 48.6 / 21.2 / 17.4 |
| **qwen27b_sql_only 7B** | **Qwen-27B, 1096 (3 сида)** | **41.03% ± 1.37** | **41.01** | 64.8 / 32.3 / 17.4 |

**Лучший результат — `qwen27b_sql_only` на target 7B = 41.03% ± 1.37** (сиды 0/1/2:
40.66 / 42.86 / 39.56), плановая основная модель. Знание в весах против знания в контексте:
**+10.3 п.п. над 7B-baseline (30.77)** при контексте в 3.2× меньше и генерации в 1.44× быстрее.
Разрыв ≈ 7.5 σ.

**3B-победитель — 30.77% ± 1.55** (сиды 31.87 / 28.57 / 31.87) против baseline 24.18 =
**+6.6 п.п.** (≈ 4 σ). Контроль `zeroshot` 5.49% показывает, что обучение переносит в веса
**+25 п.п.** — это не способность базовой модели.

> ⚠️ **Тезис «+9.5 п.п. на обоих размерах» снят.** Он держался на июльском замере и оказался
> совпадением: при пересчёте baseline 3B вырос сильнее, чем lora. Честная формулировка —
> **прирост есть на обоих размерах, но он разный: +10.3 п.п. на 7B и +6.6 п.п. на 3B.**

**Ablation (все на пересчёте, поправка на сид):**
- **Reasoning-трейсы (Think2SQL) — вредят:** 30.77 против 41.03 ± 1.37 = **−10.3 п.п.**,
  ≈ 7.5 σ. Единственный ablation с доказанным знаком. Разбор — в разделе ниже.
- **Мультизадачность (ROUTE) — НЕ доказана.** new_lora 28.57 (1 сид) **совпадает с нижним
  сидом победителя** (диапазон 28.57–31.87) и лежит внутри ±σ. На июльском замере она была
  ниже всего диапазона, откуда и брался вывод «вредит»; после пересчёта вывод **не
  подтверждается**. Направление отрицательное (на 2.2 п.п. ниже среднего = 1.4 σ), но для
  утверждения нужны сиды на самом мультизадачном арме. Из канонического датасета
  мультизадачи по-прежнему исключены — но как выбор по умолчанию, а не как доказанный вред.
- **Учитель 7B→27B — не доказан:** old_lora (июльский замер, 1 сид) внутри ±σ победителя.
  Не пересчитан, поэтому с числами этой таблицы напрямую не сравним.

Артефакты: `adapters/qwen27b_7b_sqlonly1096/` (7B-победитель), `adapters/qwen27b_sqlonly1096/` (3B);
предсказания — `results/query_results_qwen27b_{7b,sqlonly}_seed{0,1,2}.json`,
`..._{baseline,zeroshot}.json`, `query_results_qwen27b_multitask_lora.json`, `query_results_reason_lora.json`.

### Почему числа разъехались с июльскими

Июльские канонические числа (38.09 / 29.30 / 28.57 / 19.78 / 25.27 / 4.40) **не
воспроизводятся**: на тех же файлах предсказаний и том же gold каждый арм даёт на 1–4
вопроса больше.

| Арм | Июль | Пересчёт 10.08 | Δ, вопросов |
|---|---|---|---|
| zeroshot 3B | 4.40% | 5.49% | +1 |
| baseline 3B | 19.78% | 24.18% | +4 |
| sql-only 3B (3 сида) | 29.30 ± 1.37 | 30.77 ± 1.55 | +1 / +1 / +2 |
| new_lora 3B | 25.27% | 28.57% | +3 |
| baseline 7B | 28.57% | 30.77% | +2 |
| sql-only 7B (3 сида) | 38.09 ± 1.37 | 41.03 ± 1.37 | +2 / +3 / +3 |

Сдвиг **односторонний у всех армов без исключения** — то есть это свойство замера, а не
моделей. Наша сторона ни при чём: `benchmarks/evaluate_bird.py`, `uv.lock` и функция
`sqlite_to_postgres` с июля не менялись (проверено по git).

**Решающее свидетельство (11.08).** Пересчитаны шесть армов кривой объёма — те, что в июле
мерились **нашим** оценщиком. Они воспроизвелись **в точности**: 9.9→9.89, 11.0→10.99,
16.85→16.85, 15.4→15.38, 13.2→13.19, 27.84→27.84. То есть наш стенд за месяц не сдвинулся
ни на сотую. Расходятся ровно те армы, чьи июльские числа пришли **извне**. Вывод:
расхождение целиком на стороне того окружения, где снимался июльский «канон» (вероятно,
другая версия sqlglot: gold BIRD написан на SQLite, транспилируется в PostgreSQL, и gold,
который не исполнился, засчитывается провалом всем армам сразу). Точную версию установить
уже нельзя, но искать баг у себя больше не нужно.

**Что делать с этим:** числа пересчёта — единственные, которые воспроизводятся из
репозитория, поэтому канон теперь они. Выводы ветки не изменились (все разрывы сохранили
знак и запас по σ), кроме двух правок: снят тезис «+9.5 на обоих размерах» и снят вывод
«мультизадачи вредят».

⚠️ **Требует уточнения у напарницы:** наш baseline 3B при пересчёте даёт ровно **24.18%** —
то самое число, что записано в этом файле как её light-RAG baseline. И то, и другое — 22
вопроса из 91. Может быть совпадением, а может оказаться, что один замер попал в таблицу
дважды под разными именами.

## Ablation: reasoning-трейсы (Think2SQL) — отклонено

Гипотеза Think2SQL: учить target не только SQL, но и цепочке рассуждений (CoT), чтобы
вытянуть сложные запросы. Датасет `dataset/train_reasoning.json` (1096 CoT-пар от Qwen-27B),
ноутбук `kaggle_train_reasoning.ipynb`, target 7B.

| Арм (target 7B) | EX | VES | simple / moderate / challenging |
|---|---|---|---|
| baseline (схема в контексте) | 30.77% | 30.55 | 45.7 / 21.2 / 21.7 |
| **reasoning (CoT, 1 сид)** | **30.77%** | 29.51 | 48.6 / 21.2 / 17.4 |
| sql-only seed0 / 1 / 2 | 40.66 / 42.86 / 39.56% | 41.0 / 41.8 / 40.3 | |
| **sql-only (среднее 3 сидов)** | **41.03% ± 1.37** | **41.01** | 64.8 / 32.3 / 17.4 |

**Вывод: reasoning вредит — −10.3 п.п. против sql-only** (30.77 vs 41.03), разрыв ≈ **7.5 σ**,
одним сидом не объясним. Reasoning-арм **сравнялся с baseline** (30.77 в обоих) — CoT-обучение
съело всё преимущество «знания в весах»; экономия токенов (90 vs 288) осталась, прирост EX — нет.

Механизм — модель **разучилась помнить схему**. Статическая сверка предсказаний с профилями баз
(sqlglot + `profiles/<db>.json`): **34 запроса из 91 ссылаются на несуществующие колонки** против
**16 у sql-only** и 23 у baseline, то есть схемно-валидных лишь 62.6% против 82.4% — эти запросы
падают с `UndefinedColumn` до сравнения результатов. По сложности видно, где потеряли: **simple
48.6 против 64.8** (−16 п.п.) — просели именно те запросы, что решаются припоминанием имён таблиц
и колонок, а на challenging оба арма упёрлись в общий потолок 17.4%. CoT не добавил способности
рассуждать на сложных, но испортил механическое запоминание на простых: ёмкость LoRA ушла на
трейсы вместо схемы.

Это **единственный ablation ветки с доказанным знаком**. Мультизадачность (ROUTE) после
пересчёта в шум ушла, эффект учителя и фильтра тоже не доказаны — а reasoning проваливается
на 7.5 σ. Вывод для защиты: ёмкость LoRA конечна, и потраченная на рассуждения — это ёмкость,
не потраченная на схему; для per-DB SFT оптимум — чистый sql-only.

Артефакты: `results/query_results_reason_lora.json`, `dataset/train_reasoning.json`.

## Ablation ветки SFT (данные)

Пересчитано 11.08 через `reproduce_leaderboard.py --group curve` (target 3B, учитель Qwen-7B).

| Вариант | Данные | EX | VES | примечание |
|---|---|---|---|---|
| synth_50 | 50 синтетики, фильтр | 9.89% | 10.18 | 1 сид |
| synth_143 | 143 синтетики, фильтр | 10.99% | 11.22 | 1 сид |
| synth_347 | 347 синтетики, фильтр | 16.85% ± 1.37 | 17.08 ± 1.52 | 3 сида |
| unfiltered_347 | 347 без фильтра (24% битого SQL) | 15.38% | 15.09 | 1 сид |
| real_143 | 143 живых пары BIRD train | 13.19% | 13.87 | 1 сид |
| **synth_1171** | 1171 синтетики, фильтр | **27.84% ± 0.52** | 27.76 ± 0.11 | 3 сида |

**Кривая объёма: 9.89 → 10.99 → 16.85 → 27.84, плато нет** — единственный устойчивый
положительный эффект ветки (рост ×2.8 при росте данных ×23, разрыв кратно больше σ).
Фильтр (+1.5 п.п. на 347) и real vs synth — внутри шума, по одному сиду.

**Эти числа воспроизвелись в точности** (9.9→9.89, 11.0→10.99, 16.85→16.85, 15.4→15.38,
13.2→13.19, 27.84→27.84) — важная деталь для раздела о расхождении июльских чисел, см. ниже.

_σ здесь и везде в файле — по генеральной совокупности (делитель n). Прежнее «± 0.52» у
synth_1171 было посчитано с делителем n−1; приведено к единой конвенции._

## Выбор конфигурации

Критерии: EX × VES × токены × латентность × размер модели. Финальный выбор —
после сведения с веткой light-RAG и снятия VES. Победившая связка оборачивается
в MCP-сервер (см. `src/adv_text2sql/mcp_servers/text2sql_tool/`).

**Латентность генерации** (`measure_latency.py`, эндпоинт+сеть, ориентировочно):
schema-less арм (90 ток.) — median **3.69 с**, baseline (288 ток.) — **5.31 с**, т.е.
знание в весах ещё и **в 1.44× быстрее** (короче промпт → меньше prefill). Ось
«латентность» подтверждает выбор schema-less, а не только EX/токены.

_Статус:_ EX, VES, токены, **латентность** по всем армам SFT сняты; 3B и 7B на 3 сидах
(±σ); утечка вычищена; MCP smoke-тест воспроизводим; все армы пересчитаны 10.08 и
воспроизводятся из репозитория. Ablation-оси: объём данных **(+, доказан)**,
reasoning **(−10.3, доказан)**, фильтр / учитель / мультизадачи — **в шуме, не доказаны**.
Осталось: строка light-RAG и финальный выбор — на общем синке.
