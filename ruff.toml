line-length = 120

# Скрипты db2model запускаются из корня репозитория, поэтому их каталог оказывается
# на sys.path: без этой подсказки ruff считает `utils` и `llm_client` сторонними
# пакетами и мешает их с импортами библиотек.
src = [".", "db2model"]
