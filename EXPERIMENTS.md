# EXPERIMENTS — ветка db2model (SFT/LoRA)

Журнал прогонов. Каждая запись — что запускали, на чём, что получилось.
Числа без указания выборки и модели бессмысленны, поэтому они здесь всегда рядом.

Целевые базы (согласованы с веткой RAG, чтобы сравнение было честным):
`financial`, `toxicology`, `codebase_community`.

Замеры снимаются на `bird_large` (241 вопрос, 91 по нашим трём базам).
`bird_small` — 22 вопроса и целиком входит в `bird_large`, годится только для отладки.

---

## 2026-07-16 — baseline стенда

Qwen2.5-Coder-7B (эндпоинт МФТИ), схема в промпте, `bird_small`, весь стенд как есть.

| метрика | значение |
|---|---|
| EX | **22.73%** (5/22) |
| prompt-токенов на вопрос | ~2 535 |
| completion-токенов на вопрос | ~146 |

По сложности: simple 22.2%, moderate 10.0%, challenging 66.7% (на 3 вопросах — шум).

**Число не является честной точкой отсчёта.** 9 из 22 вопросов провалил стенд, а не модель:

- ambiguity-check объявил неоднозначными 4 нормальных вопроса (18.2%) → гарантированный ноль;
- `sanitize_sql()` сломал валидный SQL на 5 вопросах (qid 13, 589, 685, 1365, 1472):
  регулярка «DISTINCT + ORDER BY → подзапрос» выносит ORDER BY наружу, колонка остаётся внутри;
- gold SQL портился самим стендом (qid 1533) — исправлено, см. ниже.

2 535 prompt-токенов при схеме в ~400 — потому что на вопрос идёт ambiguity-check плюс до
`MAX_RETRIES=7` генераций, и схема летит в каждый вызов.

## 2026-07-16 — размеры схем

`_get_db_schema_light`, cl100k_base, все 8 баз BIRD: **88–891 токен**, медиана ~390.

Прямое следствие: тезис «экономим контекст, убрав схему» стоит на песке — экономить нечего.
Реальный жир не в схеме, а в ретраях и ambiguity-check.

## 2026-07-16 — исправления стенда

Найдены прогоном, не чтением. Коммиты `22d1e24..857b805`.

| что | эффект |
|---|---|
| `pyproject.toml`: пакет указывал на несуществующий `src/partial_mcp` | `uv run bird_benchmark.py` падал с ModuleNotFoundError |
| `token_tracking.py`: `total_tokens` всегда 0 | у autogen `RequestUsage` нет такого поля |
| `text2sql_implementation.py`: `final_result` не инициализирован | UnboundLocalError ронял прогон целиком |
| `evaluate_bird.py`: регулярка деления на ноль поверх sqlglot | gold превращался в `NULLIF(NULLIF,0)(x, 0)` — синтаксическая ошибка |
| `bird_evaluate_only.py` | не запускался вообще |

Проверено: gold qid 1533 теперь исполняется (10 строк); EX не изменился (модель на нём провалилась сама).

## 2026-07-17 — профиль БД

`db2model/collect_profile.py`. Схема + FK + `pg_stats` + реальные значения + примеры строк.

| БД | таблиц | FK | профиль, токенов |
|---|---|---|---|
| financial | 8 | 8 | 12 147 |
| toxicology | 4 | 3 | 2 831 |
| codebase_community | 8 | 7 | 18 771 |

Валидация (`db2model/validate_profile.py`): учитель видит **только профиль**, без доступа к БД,
и отвечает на реальные вопросы из `train_queries`. Исполнилось **4/6** на toxicology, **5/6** на financial.
Провалы — ошибки рассуждения модели, не пробелы в профиле.

## 2026-07-17 — первый сквозной прогон QLoRA

Qwen2.5-Coder-3B-Instruct, 4-bit, T4 (15.6 ГБ), toxicology, 16 пар (13 train / 3 val), 3 эпохи.

| метрика | значение |
|---|---|
| память после загрузки | 9.67 ГБ |
| пик при обучении | 10.92 ГБ |
| адаптер | 71.4 МБ |
| **EX (без схемы, 35 вопросов toxicology)** | **0.00%** |

Из 35 предсказаний: 20 — несуществующая таблица, 15 — несуществующая колонка, исполнимых ноль.

**Это не провал метода, а отсутствие данных**: 13 примеров × 3 эпохи ≈ 4 шага оптимизатора.
Ценность прогона в том, что петля замкнулась целиком: БД → профиль → учитель → фильтр → Kaggle →
предсказания → EX дома через туннель.

Проверенная и **отклонённая** гипотеза: «модель принимает db_id за схему». Префикс `toxicology.`
стоял в 13 из 35 запросов, но его снятие даёт те же 0/35 — там и колонки выдуманы.

Сравнивать этот 0% с 22.73% нельзя: другая модель, другая выборка, другой промпт.

## 2026-07-17 — генерация синтетики, объём

`db2model/generate_pairs.py` + `filter_pairs.py`, учитель Qwen2.5-Coder-7B.

| БД | сырых | чистых | брак |
|---|---|---|---|
| toxicology | 192 | 134 | **30%** |
| financial | 200 | 136 | **32%** |
| codebase_community | 200 | 138 | **31%** |

**Брак 30–32% на трёх разных базах.** Разброс в 2 процентных пункта на 592 парах — это
устойчивое свойство учителя, а не особенность одной базы. Вот это число и есть «качество teacher»,
которое требует план.

Структура брака (toxicology, n=58): `UndefinedColumn` 19, `GroupingError` 12, пустой результат 10,
`SyntaxError` 9, не парсится 3, `DivisionByZero` 2, дубликат 2, `UndefinedTable` 1.

Что дало добавление компактной выжимки схемы и явного запрета префикса в промпт учителя:

- `UndefinedTable` — с доминирующей причины до **1** случая;
- префикс `toxicology.` — с 13 запросов до **0**;
- разнообразие по сложности — с 11/4/1 до 43/73/18.

Осталось `UndefinedColumn`, в основном `t1.molecule_id`/`t2.molecule_id` на таблице `connected`,
где такой колонки нет. Это предел учителя-7B; для того и нужен фильтр по исполнению.

### Датасет

`db2model/build_dataset.py` → `db2model/dataset/` (train 347 / val 61, сплит по каждой базе отдельно,
seed 0). Состав и хеши входов — в `manifest.json`, чтобы прогон можно было привязать к данным.

| | financial | toxicology | codebase_community |
|---|---|---|---|
| пар | 136 | 134 | 138 |

По сложности суммарно: simple 148, moderate 218, challenging 42.

Отдельная находка: базы **недо-объявляют FK**. У toxicology объявлено 3, а связи
`atom.molecule_id → molecule.molecule_id` среди них нет. Правило «соединяй только по объявленным FK»
запрещало бы легальный JOIN, поэтому в промпт достраиваются неявные связи по совпадению имени
с первичным ключом (`_implied_edges`): toxicology +2, codebase_community +7, financial +0.

## 2026-07-17 — ГЛАВНЫЙ ЗАМЕР: знание в контексте против знания в весах

Первое честное сравнение. Один прогон, **одна модель** (Qwen2.5-Coder-3B-Instruct, 4-bit),
**одни вопросы** (91 из `bird_large` по трём базам), один код генерации. Отличается ровно то,
что проверяем: схема в промпте против адаптера. Обучение — 347 пар синтетики, 3 эпохи, LoRA r=16.

| Арм | схема в промпте | адаптер | **EX** | prompt-токенов |
|---|---|---|---|---|
| `baseline` | да | выключен | **23.08%** (21/91) | 288 |
| `lora` | нет | включён | **19.78%** (18/91) | 91 |

По сложности:

| | simple | moderate | challenging |
|---|---|---|---|
| baseline | 28.57% | 24.24% | 13.04% |
| lora | 31.43% | 9.09% | 17.39% |

**Что это значит.** LoRA отстаёт на 3.3 процентных пункта при контексте в 3.2 раза меньше.
Для 3B, которая схему в глаза не видела и достаёт её из весов, это неожиданно близко.
На простых вопросах она даже обходит baseline (31.4% против 28.6%), проваливаясь на moderate
(9.1% против 24.2%) — похоже, 347 примеров хватает на типовые запросы и не хватает на составные.

**Экономия токенов реальна, но ничего не стоит.** 288 → 91 это минус 68% контекста, то есть
около 200 токенов на запрос у локальной 3B. Это не деньги. Против — часы GPU на обучение.
Продавать ветку как «экономию контекста» нельзя, см. замер размеров схем выше.

### Найденный при этом баг оценки (важно для честности)

Первый подсчёт дал baseline 21.98% против lora 19.78%. Разница оказалась частично артефактом
**моего** кода: `clean_sql` снимал ограждения ` ``` ` только по краям строки, а модель без
дообучения охотно пишет «To find X, use:» и потом блок кода — вся проза уезжала в базу как SQL
и получала ноль. Таких ответов у `baseline` **9 из 91**, у `lora` — **0** (дообученная модель
выдаёт голый SQL). То есть наивный парсер штрафовал ровно тот арм, с которым сравниваемся.

Написан `db2model/extract_sql.py` (блок кода → первый `SELECT`/`WITH` → обрезка). Пересчёт:
baseline 21.98% → **23.08%**, lora 19.78% → **19.78%** (переписано 0 из 91).

Баг стоил одного вопроса, а не девяти — проза и так была в основном неверной. Но проверять было
обязательно: иначе в отчёт ушло бы число, льстящее собственной ветке из-за собственной ошибки.
В ноутбуке `clean_sql` исправлен.

## 2026-07-17 — ABLATION (неделя 5)

Пять адаптеров в одном прогоне: одна модель (3B), одни 91 вопрос, один код, меняются только
обучающие данные. EX считался локально через туннель, все предсказания прогнаны через `extract_sql.py`.

| Вариант | Данные | EX |
|---|---|---|
| synth_50 | 50 синтетики, фильтр | 9.89% |
| synth_143 | 143 синтетики, фильтр | 10.99% |
| **synth_347** | 347 синтетики, фильтр | **17.58%** |
| unfiltered_347 | 347 синтетики, **без фильтра** (24% битого SQL) | 15.38% |
| real_143 | 143 живых пары BIRD train | 13.19% |

**Три вывода, все защищаемые:**

1. **Фильтр по исполнению даёт +2.2 п.п.** synth_347 (17.58%) vs unfiltered_347 (15.38%): те же 347
   пар, разница только в фильтре. Тезис плана «фильтр обязателен» подтверждён числом. При этом даже
   нефильтрованная синтетика учится — модель частично устойчива к 24% шума, но фильтр стабильно лучше.

2. **Живые пары бьют синтетику при равном размере: +2.2 п.п.** real_143 (13.19%) vs synth_143 (10.99%),
   в обоих по 143 пары. Человеческая разметка чище синтетики от 7B — ожидаемо. НО оба варианта
   проигрывают synth_347 (17.58%): на этом масштабе количество синтетики перевешивает её меньшее
   качество. То есть учитель-7B — рабочая замена ручной разметке, если генерировать с запасом.

   При этом у real_143 виден потолок: модель копирует стилистические патологии gold-запросов BIRD.
   Вопрос 91 ушёл в 16-уровневую вложенность `SELECT` из `SELECT`; `TO_CHAR` для дат встречается 11 раз
   против 0 у синтетики. Реальные gold переусложнены, и модель перенимает это — так что живые данные
   не панацея.

3. **Данных всё ещё мало.** Кривая 9.89 → 10.99 → 17.58 растёт и не выходит на плато. Скачок именно
   143 → 347 (почти ×1.6 по EX) — синтетики нужно больше, а не меньше.

**Оговорка про шум.** synth_347 здесь 17.58%, а «полный lora» в прошлом замере 19.78% — те же 347 пар,
разброс ±2 п.п. это шум одного прогона (1 вопрос = 1.1%). Различия меньше 2-3 п.п. не обсуждать.
Для строгих выводов нужны повторные прогоны с разными сидами — оставлено на потом.

## 2026-07-18 — ПОВТОРЫ С СИДАМИ (проверка на шум)

synth_347 обучен 3 раза с сидами 0/1/2 (сид меняет инициализацию LoRA и порядок данных).
baseline детерминирован (greedy, без обучения) — один прогон. Всё на тех же 91 вопросе.

| прогон | EX |
|---|---|
| synth_347 seed0 | 15.38% |
| synth_347 seed1 | 18.68% |
| synth_347 seed2 | 16.48% |
| **synth_347 среднее** | **16.85% ± 1.37** (разброс 3.3 п.п.) |
| baseline | 19.78% |

**Главный вывод — и он корректирует более ранние.** Разброс по сидам огромный: σ = 1.37 п.п.,
между сидами 3.3 п.п. На 91 вопросе это ±1–3 ответа гуляют от одной лишь случайности обучения.

Следствия, которые надо честно проговорить на защите:

1. **Разрыв baseline vs lora реален.** 19.78% − 16.85% = 2.93 п.п. — больше 2σ. То есть baseline
   (схема в контексте) устойчиво лучше lora (схема в весах) на этой конфигурации. Тезис ветки
   «догоняем baseline» надо смягчить: 3B удерживает схему в весах, но пока проигрывает ~3 п.п.

2. **Эффект фильтра НЕ доказан.** Раньше писал «фильтр +2.2 п.п.» (synth_347 17.58 vs unfiltered 15.38).
   Но 17.58 был один сид, а по трём сидам среднее synth_347 = 16.85. Тогда эффект фильтра = +1.5 п.п.,
   что **внутри** σ = 1.37. Честный вывод: на этих данных преимущество фильтра в пределах шума,
   для доказательства нужны повторы и unfiltered-арма тоже (не сделано — квота).

3. **Вывод про размер данных (50→143→347) устойчив:** там разница 8 п.п., это заметно больше 2σ.

Урок: одиночные прогоны на 91 вопросе давали ложную уверенность. Все «эффекты» < 3 п.п. из
предыдущих записей надо читать как «в пределах шума, не доказано». Крупные эффекты (размер данных,
0% на 13 примерах) остаются в силе.

Побочно: `benchmarks/evaluate_bird.py` выполнял предсказанные запросы **без таймаута** — один
случайно-квадратичный запрос из seed2 вешал весь прогон (exit 124). Добавлен `statement_timeout=30s`.
Баг общий, задевал бы и ветку RAG.

## 2026-07-18 — VES (вторая обязательная метрика плана)

План требует три оси: EX, **VES** (Valid Efficiency Score), токены. Оценщик считал
только EX — дописан VES в `benchmarks/evaluate_bird.py`: для корректных запросов
median-тайминг gold и предсказания (5 прогонов), `R = sqrt(t_gold/t_pred)`, вклад
неверных = 0. Прогнано локально через туннель на уже сохранённых предсказаниях.

| Арм | EX | VES |
|---|---|---|
| baseline (сид-run) | 19.78% | 19.84% |
| lora seed0 / 1 / 2 | 15.38 / 18.68 / 16.48 | 15.75 / 18.40 / 16.59 |
| **lora среднее** | **16.85 ± 1.37** | **16.91 ± 1.35** |
| baseline (главный замер) | 23.08% | 22.15% |
| lora (главный замер) | 19.78% | 21.08% |

**Вывод: VES ≈ EX во всех армах.** Разница VES−EX в пределах ±1 п.п. везде. Значит
корректные предсказания исполняются примерно с той же скоростью, что gold —
эффективность не различает армы, отдельного сигнала VES не даёт. Ранжирование по VES
идентично ранжированию по EX. Единственная ось, где lora выигрывает, — токены контекста
(91 vs 288), но у локальной 3B это почти бесплатно (см. размеры схем). То есть по всем
трём осям плана честный итог тот же: baseline ≥ lora, а «экономия контекста» ничего не стоит.

Мелочь: в главном замере VES (21.08) у lora > EX (19.78) — предсказания чуть **быстрее**
gold, потому что модель пишет проще переусложнённых gold-запросов BIRD (те же 16 уровней
вложенности из real_143). Эффект есть, но крошечный.

## 2026-07-18 — МАСШТАБ ДАННЫХ ×4 + контрольный арм zeroshot

Кривая размера (9.9→11→16.85 на 50/143/347) не вышла на плато — значит синтетики мало.
Долили учителем ещё ~500 пар/база к существующим, дедуп по (question, sql):

| база | было raw | стало raw | чистых | брак |
|---|---|---|---|---|
| financial | 200 | 704 | 443 | 37% |
| toxicology | 192 | 681 | 461 | 32% |
| codebase_community | 200 | 685 | 473 | 31% |
| **итого** | 592 | 2070 | **1377** | ~33% |

Брак держится 31–37% — та же характеристика 7B-учителя, что и на первой партии.
Новый датасет `build_dataset`: **train 1171 / val 206** (было 347/61, ×3.4). По сложности
simple 487 / **moderate 786** / challenging 104 — прирост в основном в moderate, а именно
там lora проваливалась (9% против 24% у baseline). Старые ablation-варианты (synth_50/143/347)
НЕ пересобирал — иначе сломалась бы сравнимость с уже снятыми числами.

Добавлен контрольный арм **zeroshot** (3B без схемы и без адаптера) в `kaggle_train_lora.ipynb`:
отвечает на «а адаптер вообще выучил схему». Если после дообучения lora ≈ zeroshot — обучение
ничего не дало; если lora ≫ zeroshot — выучило (даже проигрывая baseline).

Правки в стенде по дороге: восстановлена затёртая ячейка `run_arm` (моя регрессия при вставке
ячейки артефакта), адаптер оформляется как zip с `adapter_card.json` (провенанс).

## 2026-07-19 — ГЛАВНЫЙ РЕЗУЛЬТАТ: масштаб данных переломил ветку

Прогон на Kaggle (train 1171, seed 42, 3 арма), EX+VES через туннель на тех же 91 вопросе.

| Арм (1171) | схема | адаптер | EX | VES | prompt-ток. |
|---|---|---|---|---|---|
| zeroshot (контроль) | нет | нет | **5.49%** | 5.35% | 90 |
| baseline | в контексте | нет | **23.08%** | 23.51% | 288 |
| **lora** | в весах | да | **29.67%** | 29.57% | 90 |

**Три вывода, все сильные:**

1. **Адаптер выучил схему — доказано контролем.** lora 29.67 vs zeroshot 5.49 = **+24.2 п.п.**
   Базовая 3B без схемы и без адаптера почти слепа (5%, выдумывает таблицы: `financial.t1`,
   `clients`…). С адаптером — реальная схема (`trans`, `account`, `connected`, `posts`) и 30% EX.
   Это снимает подозрение «16.85% было шумом базовой модели» — нет, обучение реально переносит схему.

2. **Знание в весах обошло знание в контексте.** lora 29.67 vs baseline 23.08 = **+6.6 п.п.**,
   при контексте в 3.2 раза меньше (90 vs 288 токенов). Главный тезис ветки — подтверждён числом.

3. **Объём данных решает.** lora по размеру: 9.9 (50) → 11.0 (143) → 16.85 (347) → **29.67 (1171)**.
   Скачок 347→1171 = +12.8 п.п. Кривая не выходит на плато — синтетики от 7B-учителя, если генерить
   с запасом и фильтровать исполнением, хватает, чтобы обойти baseline. VES ≈ EX (как и раньше).

**Оговорка про сид (закрыта 2026-07-20).** Эти числа — с одного сида (42). Повтор на 3 сидах дал
27.84% ± 0.64 (см. запись ниже), т.е. seed 42 (29.67) был выше среднего; знак результата надёжен
(+4.8 п.п. ≫ σ). baseline детерминирован (greedy) — его 23.08 воспроизводит главный замер.

Это переламывает более ранний честный минус («baseline на 3 п.п. лучше»): тот вывод был про
data-starved lora на 347 парах. На 1171 знак меняется.

---

## 2026-07-19 — независимая перепроверка + аудит утечки

Перепроверка главного результата с нуля (туннель, `bird_evaluate_only.py`).

- **EX воспроизведён точно** по всем трём армам: zeroshot 5.49%, baseline 23.08% (21/91),
  lora 29.67% (27/91). VES шумит от прогона к прогону (медиана wall-clock через ssh), порядок
  армов сохранён. EX-логика оценщика при добавлении VES не менялась (сверено с git до правок).
- **Утечка train→eval — найдено 2 вопроса**, вопреки прежнему «ЧИСТО». Прежняя проверка (4-грамма
  >90%) слишком груба. Строгий `check_leakage.py` (точный gold-SQL-твин с сохранением литералов):
  **q320 и q323** (toxicology) — генератор засеян реальными значениями БД (`TR000`, `TR000_1/2`).
  Например q323 «Is molecule TR000 carcinogenic» → `SELECT label FROM molecule WHERE molecule_id='TR000'`
  — lora выдаёт это буква-в-букву из обучающей пары.
- **Leak-corrected (89 вопросов, −q320,q323):** zeroshot 5.62 / baseline 22.47 / **lora 29.21**.
  Разрыв lora−baseline = **+6.7 п.п.** — не изменился (baseline тоже терял один вопрос).
  **Результат не держится на утечке.**
- **Что поправлено:** `check_leakage.py` (аудит), `dataset/train_clean.json` (1169),
  `build_dataset.py` теперь деконтаминирует автоматически; `kaggle_seeds.ipynb` переключён на train
  1171 для строгого ±σ; убраны ложные заявления «усреднено по 3 сидам» из README/REPORT/leaderboard
  (3 сида были только у ablation synth_347); ссылки на адаптер `synth347`→`synth1171`.

---

## 2026-07-20 — lora_1171 на 3 сидах: seed 42 был оптимистичным

Прогнал `kaggle_seeds.ipynb` (train.json 1171) на сидах 0/1/2, EX через туннель.

| сид | EX full (91) | EX leak-corrected (89) |
|---|---|---|
| 0 | 27.47% (25) | 26.97% (24) |
| 1 | 27.47% (25) | 28.09% (25) |
| 2 | 28.57% (26) | 29.21% (26) |
| 42 (исходный) | 29.67% (27) | 29.21% (26) |

**Среднее 3 сидов (0/1/2): 27.84% ± 0.64 (full), 28.09% ± 1.12 (leak-corrected).** Вывод: заголовочные
29.67% (seed 42) — **выше среднего**, один сид был оптимистичен. Но знак держится: 27.84 vs baseline
23.08 = **+4.8 п.п.** (≫ σ = 0.64); без утечки +5.6 п.п. Утечка почти не помогает — заученный ответ
(q323) сработал лишь на seed0. Все отчётные числа приведены к 3-сидовому среднему.

---

## 2026-07-21…07-30 — учитель 27B, target 7B, сиды, латентность, MCP (сводно)

Блок задним числом: прогоны шли, а журнал не вёлся. Первоисточники чисел — `leaderboard.md`,
`REPORT.md` и файлы в `db2model/results/`, здесь — только сводка с провенансом.

- **Учитель Qwen2.5-Coder-7B → Qwen3.6-27B** (пул МФТИ, `enable_thinking=false`). Датасет
  перегенерён: 1096 sql-only пар, брак execution-фильтра упал с 30–37% до 9–19%.
  Обоснование выбора — `db2model/PLAN_TEACHER.md`.
- **Переход на оценщик напарницы** (`evaluate_bird.py` + её `bird_large_filtered.json`) ради
  сравнимости с веткой light-RAG. baseline 3B на нём 19.78% (наш давал 23.08) — разница
  в оценщике, не в модели.
- **Ablation мультизадачности (ROUTE):** `qwen27b_multitask2699` 25.27% (1 сид) против диапазона
  победителя 27.47–30.77 → мультизадачи **отключены**, сильнее всего бьют по `codebase_community`.
- **Ablation учителя 7B→27B:** old_lora 27.47 (1 сид) попадает **внутрь** ±σ победителя
  (29.30 ± 1.37) → эффект учителя на этом объёме **в пределах шума, строго не доказан**.
  Прежнее заявление «+5.5 п.п.» держалось на высоком одиночном сиде 32.97.
- **3B на 3 сидах:** `qwen27b_sql_only` 30.77/27.47/29.67 → **29.30% ± 1.37** vs baseline 19.78 = +9.5 п.п.
- **7B (плановый основной target) на 3 сидах:** 38.46/39.56/36.26 → **38.09% ± 1.37** vs
  baseline 7B 28.57 = **+9.5 п.п.** при контексте ×3.2 меньше. Лучшее число ветки.
  Одиночный прогон давал 36.26 — на этот раз **низкий** сид (раньше seed42 был высоким):
  единичный прогон врёт в обе стороны, доверять только средним.
- **Латентность** (`measure_latency.py`, эндпоинт+сеть): schema-less 3.69 с median против
  baseline 5.31 с → **×1.44 быстрее** (короче промпт → меньше prefill). Ось отбора закрыта.
- **MCP-сервер:** `mcp_smoke_test.py`, живой путь `query()` → LLM → SQL → исполнение на БД,
  **3/3 PASS** по базам в режиме `with_schema=True`. Режим `with_schema=False` проходит по коду,
  но без адаптера в проде базовая модель галлюцинирует имена таблиц — адаптер через vLLM
  не отдаётся, это остаётся незакрытым инженерным шагом.

---

## 2026-08-10 — ABLATION: reasoning-трейсы (Think2SQL) — отклонено, −10.3 п.п.

Гипотеза Think2SQL: учить target цепочке рассуждений (CoT) + SQL, чтобы вытянуть сложные запросы.
Датасет `dataset/train_reasoning.json` (1096 CoT-пар от Qwen-27B), `kaggle_train_reasoning.ipynb`,
target 7B, 1 сид. Замер — **нашим** оценщиком (`data/bird_large.json`), поэтому тем же оценщиком
пересняты и остальные 7B-армы; с числом 38.09 (оценщик напарницы) не смешивать.

| Арм (target 7B) | EX | VES | simple / moderate / challenging |
|---|---|---|---|
| baseline (схема в контексте) | 30.77% | 30.38 | 45.7 / 21.2 / 21.7 |
| **reasoning (CoT, 1 сид)** | **30.77%** | 29.06 | 48.6 / 21.2 / 17.4 |
| sql-only seed0 / 1 / 2 | 40.66 / 42.86 / 39.56% | 41.4 / 41.8 / 40.4 | |
| **sql-only (среднее 3 сидов)** | **41.03% ± 1.37** | 41.20 | 64.8 / 32.3 / 17.4 |

**Reasoning −10.3 п.п. против sql-only** (30.77 vs 41.03) — это ≈ **7.5 σ**, одним сидом не
объяснить. Reasoning **сравнялся с baseline** (30.77 в обоих): CoT-обучение съело всё преимущество
«знания в весах», экономия токенов (90 vs 288) осталась, прирост EX — нет.

**Механизм — модель разучилась помнить схему.** Статическая сверка предсказаний с профилями баз
(sqlglot + `profiles/<db>.json`, без обращения к БД): **34 запроса из 91 ссылаются на
несуществующие колонки** против **16 у sql-only** и 23 у baseline; схемно-валидных 62.6% против
82.4%. Такие запросы падают с `UndefinedColumn` ещё до сравнения результатов. Разбивка по
сложности показывает, где потеряли: **simple 48.6 против 64.8** (−16 п.п.) — просели ровно те
запросы, что решаются припоминанием имён таблиц и колонок; на challenging оба арма упёрлись в
общий потолок 17.4%. CoT не добавил умения рассуждать на сложных, но испортил механическое
запоминание на простых — ёмкость LoRA ушла на трейсы вместо схемы.

Примеры галлюцинаций: q122 `SELECT A2, A3 FROM loan` (колонки `a1/a2/a3` живут в `district`),
q179 `FROM trans WHERE client_id=617` (в `trans` нет `client_id`), q708 `u.creaiondate` (опечатка
в имени колонки), q279 `SELECT label FROM bond` (`label` в `molecule`).

Вместе с ROUTE — **второй отрицательный ablation в одну сторону**: ни мультизадачность (−2 п.п.),
ни reasoning (−10.3 п.п.) не помогают per-DB SFT. Всё, что отвлекает ёмкость адаптера от
запоминания конкретной схемы, вредит; чистый sql-only — оптимум. Обе популярные техники из
опорных статей проверены на своём стенде и отклонены числом.

Артефакты: `results/query_results_reason_lora.json`.

---

## 2026-08-10 — ПЕРЕСЧЁТ ВСЕХ АРМОВ: июльские канонические числа не воспроизводятся

Напарница передала `bird_large_filtered.json` (положен в `data/`). Выяснилось, что это **те же
91 вопрос**, что и в `data/bird_large.json` — подмножество, gold-SQL совпадает буква в букву,
id вложены. Оба файла дают идентичный EX. Версия «два оценщика, разница в замере», на которой
строились сравнения в документах, **оказалась неверной**: оценщик один.

Зато обнаружилось другое: **июльские канонические числа не воспроизводятся**. На тех же файлах
предсказаний и том же gold каждый арм даёт больше.

| Арм | Июль | Пересчёт 10.08 | Δ, вопросов |
|---|---|---|---|
| zeroshot 3B | 4.40% | **5.49%** | +1 |
| baseline 3B | 19.78% | **24.18%** | +4 |
| sql-only 3B (3 сида) | 29.30 ± 1.37 | **30.77 ± 1.55** (31.87/28.57/31.87) | +1/+1/+2 |
| new_lora 3B (мультизадачи) | 25.27% | **28.57%** | +3 |
| baseline 7B | 28.57% | **30.77%** | +2 |
| sql-only 7B (3 сида) | 38.09 ± 1.37 | **41.03 ± 1.37** (40.66/42.86/39.56) | +2/+3/+3 |
| reasoning 7B | — | **30.77%** | — |

Сдвиг **односторонний у всех армов без исключения** → свойство стенда, не моделей. Проверено
по git: `benchmarks/evaluate_bird.py`, `uv.lock` и `sqlite_to_postgres` с июля не менялись,
sqlglot 28.6.0. Правдоподобное объяснение — июльские числа снимались в другом окружении
(у напарницы), с другими версиями библиотек: gold BIRD транспилируется SQLite→PostgreSQL через
sqlglot, и gold, который не исполнился, засчитывается провалом всем армам сразу. **Строго
причина не установлена.** Канон — пересчёт, как единственные воспроизводимые из репозитория числа.

**Две правки выводов (обе — в минус нашим прежним заявлениям):**

1. **Снят тезис «+9.5 п.п. на обоих размерах».** baseline 3B при пересчёте вырос сильнее, чем
   lora, и симметрия развалилась: **7B +10.3 п.п.** (41.03 vs 30.77), **3B +6.6 п.п.**
   (30.77 vs 24.18). Тезис ветки держится с запасом по σ на обоих размерах, но «одинаковые
   +9.5» были совпадением конкретного замера, а не свойством метода. Готовился на слайд — убрать.
2. **Снят вывод «мультизадачность вредит».** new_lora 28.57 (1 сид) **совпадает с нижним сидом
   победителя** (диапазон 28.57–31.87) и лежит внутри ±σ. В июле она была ниже всего диапазона —
   отсюда и брался вывод. Теперь честно: направление отрицательное (−2.2 п.п. от среднего = 1.4 σ),
   но не доказано; нужны сиды на самом мультизадачном арме. Мультизадачи остаются выключенными
   как выбор по умолчанию, а не как доказанный вред.

После этого **единственный ablation ветки с доказанным знаком — reasoning** (−10.3 п.п., ≈7.5 σ).
Объём данных остаётся доказанным положительным фактором. Фильтр, учитель и мультизадачи — в шуме.

⚠️ **Вопрос напарнице:** наш baseline 3B при пересчёте даёт ровно **24.18%** — то самое число,
что записано в лидерборде как её light-RAG baseline. Оба = 22 вопроса из 91. Совпадение или
один замер, попавший в таблицу дважды под разными именами?

**Грабли стенда (для тех, кто будет повторять):** оценщик исполняет и gold, и предсказание с
`statement_timeout=30000`, поэтому слабый арм (много запросов-долгожителей) считается ~45–60 минут.
Отдельно: если ssh-туннель падает посреди прогона, процессы **не падают, а виснут навсегда** на
уже установленных сокетах (TCP keepalive по умолчанию 2 часа). Признак — аптайм растёт, а CPU
стоит на нуле. Проверять живость надо по CPU и `pg_stat_activity`, не по аптайму.

---

## 2026-08-11 — закрытие трёх дыр по DoD + стенд оправдан

Построчная сверка с ТЗ (а не со своим списком) вскрыла три невыполненных пункта. Все закрыты.

**1. VES 3B-победителя не был снят** — обязательная метрика плана, в лидерборде стоял прочерк.
Снят: `qwen27b_sql_only` 3B = **EX 30.77 ± 1.55, VES 31.10 ± 1.10** (3 сида).

**2. Кривая объёма данных не пересчитывалась** после смены канона — а на ней держится
единственный доказанный положительный вывод ветки. Пересчитана целиком:

| Вариант | EX | VES |
|---|---|---|
| synth_50 | 9.89% | 10.18 |
| synth_143 | 10.99% | 11.22 |
| synth_347 (3 сида) | 16.85% ± 1.37 | 17.08 ± 1.52 |
| unfiltered_347 | 15.38% | 15.09 |
| real_143 | 13.19% | 13.87 |
| synth_1171 (3 сида) | 27.84% ± 0.52 | 27.76 ± 0.11 |

**3. Не было воспроизведения одной командой** (DoD нед.1) — при том, что в документах это
утверждалось. Написан `db2model/reproduce_leaderboard.py`: гоняет все армы через единый
оценщик, печатает markdown-таблицы, считает ±σ по сидам, флаги `--only/--group/--out`.

### Главное: стенд оправдан

Шесть армов кривой мерились в июле **нашим** оценщиком — и воспроизвелись **в точности**
(9.9→9.89, 11.0→10.99, 16.85→16.85, 15.4→15.38, 13.2→13.19, 27.84→27.84). Значит наш стенд
за месяц не сдвинулся ни на сотую, а расходятся ровно те армы, чьи июльские числа пришли
извне. Расхождение канона — целиком на стороне чужого окружения; искать баг у себя больше
не нужно.

### Побочно

- **Найдена причина падений ssh-туннеля:** `create_engine` вызывался ВНУТРИ цикла по вопросам,
  то есть прогон открывал ~180 TCP-соединений через туннель и сам же его ронял. Исправлено:
  один engine на базу (3 на прогон) + `pool_pre_ping` + `connect_timeout=15`, чтобы при обрыве
  падать с ошибкой, а не виснуть навсегда. EX не изменился (сверено на 3B-сидах: 30.77 до и после),
  прогон трёх сидов ускорился с десятков минут до нескольких.
- **Утечка перепроверена на данных победителя** (91 вопрос против 1096 пар): 0 контаминированных.
  Прежний аудит делался на старом наборе 1171.
- **σ приведена к одной конвенции** (генеральная совокупность, делитель n). Прежнее «± 0.64» у
  synth_1171 считалось с делителем n−1; теперь везде ± 0.52 и т.д.

---

## Открытые вопросы

- **Слайды защиты не сделаны** (DoD недели 6) — единственный полностью «наш» невыполненный пункт.
- **Строка light-RAG в лидерборде** — за напарницей; общий split и её baseline (24.18) уже сведены.
- **Почему июльские числа не воспроизводятся** — причина не установлена (см. запись 10.08).
  Если найдётся окружение напарницы, стоит сверить версии sqlglot/psycopg.
- **Сиды на мультизадачном арме и на old_lora** — без них ни вред мультизадач, ни эффект
  учителя не отделить от шума.
- **Адаптер не отдаётся через vLLM** — schema-less арм внутри MCP работает по коду, но без
  адаптера даёт галлюцинации; end-to-end продемонстрирован только `with_schema=True`.
- **Teacher GPT-класса не выдали** (внешний API под разметку). 27B — потолок в рамках ограничений;
  переезд на DeepSeek-V3 при появлении ключа = 3 значения в `.env`, код менять не надо.
- **Эффект учителя 7B→27B строго не доказан** — old_lora (1 сид) внутри ±σ победителя.
  Нужны сиды на old_lora, чтобы разделить вклад учителя и вклад объёма данных.
- **`train_queries.json` (357 реальных пар, 143 по нашим базам) не задействован.** Это буквально
  обучающий набор из ROUTE и естественная верхняя оценка качества синтетики.
- **Tool-calling на эндпоинте выключен** (`--enable-auto-tool-choice` не задан) — агентный baseline
  стенда на нём не заведётся. Ветке db2model не мешает, куратору сказать стоит.
- **ROUTE читается не так, как записано в плане**: там не учитель генерирует пары, а берутся
  реальные SPIDER+BIRD и из них синтезируются 4 формата задач (TS/SL/NC/CW). MSFT в план не заложен.
